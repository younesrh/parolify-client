import styled from "styled-components";

const Home = styled.main`
  .hero-title {
    max-width: 600px;
    text-align: center;
    margin: 0 auto;
    margin-top: 64px;
  }
`;

export const Styled = {
  Home,
};
